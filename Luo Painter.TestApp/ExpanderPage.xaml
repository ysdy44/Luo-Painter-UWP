<Page
    x:Class="Luo_Painter.TestApp.ExpanderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Luo_Painter.TestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <ToggleButton x:Name="ToggleButton" HorizontalAlignment="Center" VerticalAlignment="Center" >
            On/Off
        </ToggleButton>
        <ToggleButton x:Name="LeftToggleButton" HorizontalAlignment="Left" VerticalAlignment="Center" >
            On/Off
        </ToggleButton>
        <ToggleButton x:Name="TopToggleButton" HorizontalAlignment="Center" VerticalAlignment="Top" >
            On/Off
        </ToggleButton>
        <ToggleButton x:Name="RightToggleButton" HorizontalAlignment="Right" VerticalAlignment="Center" >
            On/Off
        </ToggleButton>
        <ToggleButton x:Name="BottomToggleButton" HorizontalAlignment="Center" VerticalAlignment="Bottom" >
            On/Off
        </ToggleButton>
    </Grid>
</Page>