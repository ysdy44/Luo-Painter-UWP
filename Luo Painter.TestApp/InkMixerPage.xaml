<Page
    x:Class="Luo_Painter.TestApp.InkMixerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:transformers="using:FanKit.Transformers"
    xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <FrameworkElement.Resources>
        <!--Transformer-->
        <transformers:CanvasOperator x:Name="Operator" DestinationControl="{x:Bind CanvasControl}"/>
        <transformers:CanvasTransformer x:Name="Transformer" />
    </FrameworkElement.Resources>

    <Grid>

        <xaml:CanvasControl x:Name="CanvasControl"/>

        <StackPanel Margin="12,62" Padding="18,12" HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="{ThemeResource AppBarBackgroundThemeBrush}" >
        
            <TextBlock>
                <Run Foreground="OrangeRed">Paint Color</Run>
                <Run x:Name="XRun" Foreground="OrangeRed">0</Run>
                <Run Foreground="OrangeRed">%</Run>
            </TextBlock>
            <Slider x:Name="XSlider" Value="0" Width="200" Foreground="OrangeRed"/>
        
            <TextBlock>
                <Run Foreground="YellowGreen">Mix Color</Run>
                <Run x:Name="YRun" Foreground="YellowGreen">100</Run>
                <Run Foreground="YellowGreen">%</Run>
            </TextBlock>
            <Slider x:Name="YSlider" Value="100" Width="200" Foreground="YellowGreen"/>
         
            <TextBlock>
                <Run Foreground="DodgerBlue">Persistence Color</Run>
                <Run x:Name="ZRun" Foreground="DodgerBlue">1</Run>
                <Run Foreground="DodgerBlue">%</Run>
            </TextBlock>
            <Slider x:Name="ZSlider" Value="0" Width="200" Foreground="DodgerBlue"/>
            
        </StackPanel>

        <CommandBar Margin="12" HorizontalAlignment="Center" VerticalAlignment="Bottom" >

            <AppBarButton>
                <Button.Flyout>
                    <Flyout>
                        <ColorPicker
                            x:Name="ColorPicker"
                            Color="White"
                            ColorSpectrumShape="Ring"
                            IsMoreButtonVisible="False"
                            IsColorSliderVisible="False"
                            IsColorChannelTextInputVisible="False"
                            IsHexInputVisible="False"
                            IsAlphaEnabled="False"
                            IsAlphaSliderVisible="True"
                            IsAlphaTextInputVisible="True" />
                    </Flyout>
                </Button.Flyout>
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="FontColor"/>
                </AppBarButton.Icon>
                <AppBarButton.Label>
                    Color
                </AppBarButton.Label>
            </AppBarButton>

            <AppBarButton x:Name="ClearButton" >
                <UIElement.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="None" Key="Delete"/>
                </UIElement.KeyboardAccelerators>
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Delete"/>
                </AppBarButton.Icon>
                <AppBarButton.Label>
                    Clear
                </AppBarButton.Label>
            </AppBarButton>

        </CommandBar>

    </Grid>
</Page>