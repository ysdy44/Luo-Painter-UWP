<local:CurveSelector
    x:Class="Luo_Painter.HSVColorPickers.CurveSelectorWithUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Luo_Painter.HSVColorPickers"
    d:DesignHeight="400"
    d:DesignWidth="400"
    mc:Ignorable="d"
    Background="Transparent"
    >

    <local:CurveSelector.ItemStyle>
        <Style TargetType="Button" BasedOn="{StaticResource TextBlockButtonStyle}">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Foreground" Value="{ThemeResource AppBarItemForegroundThemeBrush}"/>
        </Style>
    </local:CurveSelector.ItemStyle>
    <local:CurveSelector.ItemTemplate>
        <DataTemplate>
            <Grid>
                <Ellipse Width="20" Height="20" Fill="{ThemeResource AppBarBackgroundThemeBrush}"/>
                <FontIcon Glyph="&#xECCA;" FontFamily="Segoe MDL2 Assets"/>
            </Grid>
        </DataTemplate>
    </local:CurveSelector.ItemTemplate>
</local:CurveSelector>