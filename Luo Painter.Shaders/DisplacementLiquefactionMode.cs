namespace Luo_Painter.Shaders
{
    public enum DisplacementLiquefactionMode
    {
        Reset = 0,

        Push = 1,

        Pinch = 2,
        Expand = 3,

        TwirlLeft = 4,
        TwirlRight = 5,

        // Reset = 6,
        // Reset = 7,
        // Reset = 8,
        // Reset = ...
    }
}