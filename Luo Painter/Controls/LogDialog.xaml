<ContentDialog
    x:Class="Luo_Painter.Controls.LogDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:luo_painter="using:Luo_Painter"
    xmlns:strings="using:Luo_Painter.Strings"
    d:DesignHeight="400"
    d:DesignWidth="400"
    mc:Ignorable="d"
    Style="{StaticResource AppContentDialogStyle}"
    PrimaryButtonText="{strings:UI Type=Back}"
    Title="{strings:UI Type=Log}"
    >

    <ScrollViewer>
        <StackPanel x:Name="StackPanel" Margin="12,0" Spacing="10">

            <Panel.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True"/>
                </TransitionCollection>
            </Panel.ChildrenTransitions>

            <!--TextBlock-->
            
        </StackPanel>
    </ScrollViewer>
</ContentDialog>