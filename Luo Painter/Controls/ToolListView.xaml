<Canvas
    x:Class="Luo_Painter.Controls.ToolListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Luo_Painter.Controls"
    xmlns:tools="using:Luo_Painter.Tools"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50"  
    Margin="0,49,0,0" 
    Width="0" 
    Height="Auto"
    MinHeight="100" 
    HorizontalAlignment="Left"
    VerticalAlignment="Stretch" 
    Background="{ThemeResource SystemControlAcrylicElementBrush}"
    >

    <FrameworkElement.Resources>
        <!--Storyboard-->
        <Storyboard x:Name="HideStoryboard" >
            <DoubleAnimation Duration="0:0:0.1" By="{x:Null}" To="{x:Bind ReverseDoubleConverter(Width), Mode=OneWay}" Storyboard.TargetProperty="X" Storyboard.TargetName="TranslateTransform" d:IsOptimized="True"/>
        </Storyboard>
        <Storyboard x:Name="ShowStoryboard" >
            <DoubleAnimation Duration="0:0:0.15" By="{x:Null}" To="0" Storyboard.TargetProperty="X" Storyboard.TargetName="TranslateTransform" d:IsOptimized="True"/>
        </Storyboard>
        <!--CollectionViewSource-->
        <CollectionViewSource x:Name="ToolCollectionViewSource" IsSourceGrouped="True">
            <CollectionViewSource.Source>
                <local:ToolGroupingList x:Name="ToolCollection">
                    <local:ToolGrouping Key="Marquee">
                        <tools:ToolType>MarqueeRectangular</tools:ToolType>
                        <tools:ToolType>MarqueeElliptical</tools:ToolType>
                        <tools:ToolType>MarqueePolygon</tools:ToolType>
                        <tools:ToolType>MarqueeFreeHand</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Selection">
                        <tools:ToolType>SelectionFlood</tools:ToolType>
                        <tools:ToolType>SelectionBrush</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Paint">
                        <tools:ToolType>PaintBrush</tools:ToolType>
                        <tools:ToolType>PaintWatercolorPen</tools:ToolType>
                        <tools:ToolType>PaintPencil</tools:ToolType>
                        <tools:ToolType>PaintEraseBrush</tools:ToolType>
                        <tools:ToolType>PaintLiquefaction</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Vector">
                        <tools:ToolType>Cursor</tools:ToolType>
                        <tools:ToolType>View</tools:ToolType>
                        <tools:ToolType>Brush</tools:ToolType>
                        <tools:ToolType>Transparency</tools:ToolType>
                        <tools:ToolType>Image</tools:ToolType>
                        <tools:ToolType>Crop</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Text">
                        <tools:ToolType>TextArtistic</tools:ToolType>
                        <tools:ToolType>TextFrame</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Curve">
                        <tools:ToolType>Pen</tools:ToolType>
                        <tools:ToolType>Node</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Geometry">
                        <!--Geometry0-->
                        <tools:ToolType>GeometryRectangle</tools:ToolType>
                        <tools:ToolType>GeometryEllipse</tools:ToolType>
                        <!--Geometry1-->
                        <tools:ToolType>GeometryRoundRect</tools:ToolType>
                        <tools:ToolType>GeometryTriangle</tools:ToolType>
                        <tools:ToolType>GeometryDiamond</tools:ToolType>
                        <!--Geometry2-->
                        <tools:ToolType>GeometryPentagon</tools:ToolType>
                        <tools:ToolType>GeometryStar</tools:ToolType>
                        <tools:ToolType>GeometryCog</tools:ToolType>
                        <!--Geometry3-->
                        <tools:ToolType>GeometryDount</tools:ToolType>
                        <tools:ToolType>GeometryPie</tools:ToolType>
                        <tools:ToolType>GeometryCookie</tools:ToolType>
                        <!--Geometry4-->
                        <tools:ToolType>GeometryArrow</tools:ToolType>
                        <tools:ToolType>GeometryCapsule</tools:ToolType>
                        <tools:ToolType>GeometryHeart</tools:ToolType>
                    </local:ToolGrouping>
                    <local:ToolGrouping Key="Pattern">
                        <tools:ToolType>PatternGrid</tools:ToolType>
                        <tools:ToolType>PatternDiagonal</tools:ToolType>
                        <tools:ToolType>PatternSpotted</tools:ToolType>
                    </local:ToolGrouping>
                </local:ToolGroupingList>
            </CollectionViewSource.Source>
        </CollectionViewSource>
    </FrameworkElement.Resources>

    <UIElement.RenderTransformOrigin>
        0.5,0.5
    </UIElement.RenderTransformOrigin>
    <UIElement.RenderTransform>
        <TranslateTransform x:Name="TranslateTransform" X="0"/>
    </UIElement.RenderTransform>

    <ListView x:Name="ListView" 
              IsItemClickEnabled="True" 
              SelectionMode="Single" 
              
              ScrollViewer.VerticalScrollBarVisibility="Hidden"
              ItemsSource="{x:Bind ToolCollectionViewSource.View}" 
              
              Canvas.Left="{x:Bind ListViewLeftConverter(Width), Mode=OneWay}" 
              Canvas.Top="0"
              Width="{x:Bind ListViewWidthConverter(Width), Mode=OneWay}" 
              Height="0"
              Visibility="{x:Bind DoubleToVisibilityConverter(Width), Mode=OneWay}" 
              >

        <ItemsControl.ItemContainerTransitions>
            <TransitionCollection>

            </TransitionCollection>
        </ItemsControl.ItemContainerTransitions>

        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsWrapGrid                   
                    ItemWidth="70"
                    ItemHeight="50"
                   
                    Orientation="Horizontal"  
                    MaximumRowsOrColumns="-1" 
                    
                    AreStickyGroupHeadersEnabled="False"   
                    GroupHeaderPlacement="Top"          
                    GroupPadding="0"    
                    />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="MinWidth" Value="55"/>
                <Setter Property="Height" Value="55"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Padding" Value="2,1"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Transitions">
                    <Setter.Value>
                        <TransitionCollection>

                        </TransitionCollection>
                    </Setter.Value>
                </Setter>
            </Style>
        </ItemsControl.ItemContainerStyle>

        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="tools:ToolType">
                <local:ToolIcon Type="{x:Bind}"/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>

        <ItemsControl.GroupStyle>
            <GroupStyle HidesIfEmpty="True">
                <GroupStyle.HeaderContainerStyle>
                    <Style TargetType="ListViewHeaderItem">
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="MinHeight" Value="2"/>
                        <Setter Property="Height" Value="Auto"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewHeaderItem">
                                    <Rectangle Height="2" Fill="{ThemeResource AppStroke}" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </GroupStyle.HeaderContainerStyle>
            </GroupStyle>
        </ItemsControl.GroupStyle>

    </ListView>

    <Line
          IsHitTestVisible="False" 
          X1="{x:Bind ThumbeLeftConverter(Width), Mode=OneWay}"
          X2="{x:Bind ThumbeLeftConverter(Width), Mode=OneWay}" 
          Y1="0"
          Y2="{x:Bind ThumbeTopConverter(ListView.Height), Mode=OneWay}" 
          StrokeThickness="1" 
          Stroke="{ThemeResource AppStroke}" 
          />

    <Line
          IsHitTestVisible="False" 
          X1="{x:Bind ThumbeLeftConverter(Width), Mode=OneWay}"
          X2="{x:Bind ThumbeLeftConverter(Width), Mode=OneWay}" 
          Y1="{x:Bind ThumbeTopConverter(ListView.Height), Mode=OneWay}" 
          Y2="{x:Bind ListView.Height, Mode=OneWay}"
          StrokeThickness="1"
          Stroke="{ThemeResource AppStroke}" 
          />

    <Border
            CornerRadius="0,8,8,0" 
            Canvas.Left="{x:Bind ThumbeLeftConverter(Width), Mode=OneWay}" 
            Canvas.Top="{x:Bind ThumbeTopConverter(ListView.Height), Mode=OneWay}" 
            Width="22" 
            Height="120"
            BorderThickness="0,1,1,1" 
            BorderBrush="{ThemeResource AppStroke}" 
            Background="{ThemeResource SystemControlAcrylicElementBrush}" 
            Visibility="{x:Bind IsShow,Mode=OneWay}"           
            >
        <Thumb x:Name="Thumb" >
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Grid Background="Transparent">

                        <!--VisualState-->
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="BackgroundPointerOver" />
                                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="Background" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="BackgroundPressed" />
                                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="Background" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Border x:Name="Background"                              
                                IsHitTestVisible="False"                           
                                Width="6" 
                                Height="36" 
                                Margin="3" 
                                VerticalAlignment="Center" 
                                CornerRadius="{TemplateBinding CornerRadius}" 
                                BorderThickness="{TemplateBinding BorderThickness}"            
                                BorderBrush="{TemplateBinding BorderBrush}"            
                                Background="{TemplateBinding Background}"           
                                />
                        <Border x:Name="BackgroundPointerOver"                            
                                Opacity="0" 
                                IsHitTestVisible="False"
                                Width="6" 
                                Height="36" 
                                Margin="3" 
                                VerticalAlignment="Center" 
                                CornerRadius="{TemplateBinding CornerRadius}"          
                                BorderThickness="{TemplateBinding BorderThickness}"              
                                BorderBrush="{ThemeResource ThumbBorderBrushPointerOver}"             
                                Background="{ThemeResource ThumbBackgroundPointerOver}"              
                                />
                        <Border x:Name="BackgroundPressed"                              
                                Opacity="0" 
                                IsHitTestVisible="False"
                                Width="6" 
                                Height="36" 
                                Margin="3" 
                                VerticalAlignment="Center" 
                                CornerRadius="{TemplateBinding CornerRadius}"          
                                Background="{ThemeResource ThumbBackgroundPressed}"             
                                BorderBrush="{ThemeResource ThumbBorderBrushPressed}"              
                                BorderThickness="{TemplateBinding BorderThickness}"                
                                />
                    </Grid>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Border>

</Canvas>