<Canvas
    x:Class="Luo_Painter.Controls.LayerListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Luo_Painter.Controls"
    xmlns:elements="using:Luo_Painter.Elements"
    xmlns:layers="using:Luo_Painter.Layers"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50"  
    Margin="0,49,0,0" 
    Width="0" 
    Height="Auto"
    MinHeight="100" 
    HorizontalAlignment="Right"
    VerticalAlignment="Stretch" 
    Background="{ThemeResource SystemControlAcrylicElementBrush}"
    >

    <FrameworkElement.Resources>
        <!--Storyboard-->
        <Storyboard x:Name="HideStoryboard" >
            <DoubleAnimation Duration="0:0:0.1" By="{x:Null}" To="{x:Bind Width, Mode=OneWay}" Storyboard.TargetProperty="X" Storyboard.TargetName="TranslateTransform" d:IsOptimized="True"/>
        </Storyboard>
        <Storyboard x:Name="ShowStoryboard" >
            <DoubleAnimation Duration="0:0:0.15" By="{x:Null}" To="0" Storyboard.TargetProperty="X" Storyboard.TargetName="TranslateTransform" d:IsOptimized="True"/>
        </Storyboard>
    </FrameworkElement.Resources>

    <UIElement.RenderTransformOrigin>
        0.5,0.5
    </UIElement.RenderTransformOrigin>
    <UIElement.RenderTransform>
        <TranslateTransform x:Name="TranslateTransform" X="0"/>
    </UIElement.RenderTransform>

    <ListView x:Name="ListView" 
              IsItemClickEnabled="True"
              SelectionMode="Extended"   
              
              AllowDrop="True" 
              CanDragItems="True"
              CanReorderItems="True" 
              ReorderMode="Enabled"   
           
              ScrollViewer.VerticalScrollBarVisibility="Hidden"
              
              Canvas.Left="0" 
              Canvas.Top="0"
              Width="{x:Bind ListViewWidthConverter(Width), Mode=OneWay}" 
              Height="0"
              Visibility="{x:Bind DoubleToVisibilityConverter(Width), Mode=OneWay}" 
              >

        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsStackPanel VerticalAlignment="Bottom"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="MinWidth" Value="70"/>
                <Setter Property="Height" Value="55"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Padding" Value="2,1"/>
                <Setter Property="Margin" Value="0,1"/>
                <Setter Property="Background" Value="{ThemeResource ComboBoxItemPointerOverBackgroundThemeBrush}"/>
                <Setter Property="Transitions">
                    <Setter.Value>
                        <TransitionCollection>

                        </TransitionCollection>
                    </Setter.Value>
                </Setter>
            </Style>
        </ItemsControl.ItemContainerStyle>

        <ItemsControl.ItemContainerTransitions>
            <TransitionCollection>

            </TransitionCollection>
        </ItemsControl.ItemContainerTransitions>

        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="layers:ILayer">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="66"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="2" IsHitTestVisible="False" VerticalAlignment="Center" 
                                Visibility="{Binding ElementName=SelectedButton, Path=Visibility, Mode=OneWay}" >
                        <Run Text="{x:Bind Name, Mode=OneWay}"/>                                   
                        <Run>
                            (
                        </Run><Run Text="{x:Bind Type, Mode=OneWay}"/><Run>                              
                            )       
                        </Run>
                    </TextBlock>

                    <FontIcon Grid.Column="3" IsHitTestVisible="False" Opacity="0.8" Glyph="&#x23A5;" FontSize="18" FontFamily="Segoe UI Symbol"
                               Visibility="{Binding ElementName=SelectedButton, Path=Visibility, Mode=OneWay}" />

                    <elements:SelectedButtonPresenter Visibility="{Binding ElementName=SelectedButton, Path=Visibility, Mode=OneWay}"/>
                    <elements:VisibleButton Grid.Column="4"
                                Visible="{x:Bind Visibility, Mode=OneWay}"
                                CommandParameter="{Binding}" 
                                Command="{Binding ElementName=VisualButton, Path=Command, Mode=OneWay}"
                                Visibility="{Binding ElementName=SelectedButton, Path=Visibility, Mode=OneWay}"
                                />

                    <Border Grid.Column="1" IsHitTestVisible="False" BorderThickness="1" BorderBrush="Black" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image Source="{x:Bind Thumbnail, Mode=OneWay}" />
                    </Border>

                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>

        <ListView.Header>
            <Grid Height="55" Background="{ThemeResource AppBackgroundLight}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="69"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="MarqueeTextBlock" Grid.Column="2" IsHitTestVisible="False" VerticalAlignment="Center" 
                                Visibility="{x:Bind SelectedVisibilityConverter(Width), Mode=OneWay}" >
                    Marquee
                </TextBlock>
                <Border Grid.Column="1" IsHitTestVisible="False" BorderThickness="1" BorderBrush="Black" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image x:Name="Image"/>
                </Border>
                <ContentControl x:Name="SelectedButton" Width="31" Foreground="Gray" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                Visibility="{x:Bind SelectedVisibilityConverter(Width), Mode=OneWay}" >
                    <SymbolIcon Symbol="SelectAll" />
                </ContentControl>
            </Grid>
        </ListView.Header>

        <ListView.Footer>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="VisualButton" Visibility="Collapsed" 
                    CommandParameter="{x:Null}">
                    <Button.Command>
                        <local:LayerCommand x:Name="VisualCommand"/>
                    </Button.Command>
                    <SymbolIcon Symbol="View"/>
                </Button>
                <Button x:Name="AddButton" Style="{StaticResource AppButtonStyle}">
                    <UIElement.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="None" Key="Insert"/>
                    </UIElement.KeyboardAccelerators>
                    <SymbolIcon Symbol="Add"/>
                </Button>
                <Button x:Name="ImageButton" Grid.Column="1" Style="{StaticResource AppButtonStyle}">
                    <SymbolIcon Symbol="Pictures"/>
                </Button>
                <Button x:Name="RemoveButton" Grid.Column="3" Style="{StaticResource AppButtonStyle}">
                    <SymbolIcon Symbol="Delete"/>
                </Button>
            </Grid>
        </ListView.Footer>

    </ListView>

    <Line
          IsHitTestVisible="False" 
          X1="0"
          X2="0"
          Y1="0"
          Y2="{x:Bind ThumbeTopConverter(ListView.Height), Mode=OneWay}" 
          StrokeThickness="1" 
          Stroke="{ThemeResource AppBarBorderThemeBrush}" 
          />

    <Line
          IsHitTestVisible="False" 
          X1="0"
          X2="0"
          Y1="{x:Bind ThumbeBottomConverter(ListView.Height), Mode=OneWay}" 
          Y2="{x:Bind ListView.Height, Mode=OneWay}"
          StrokeThickness="1"
          Stroke="{ThemeResource AppBarBorderThemeBrush}" 
          />

    <Border
            CornerRadius="8,0,0,8" 
            Canvas.Left="-22" 
            Canvas.Top="{x:Bind ThumbeTopConverter(ListView.Height), Mode=OneWay}" 
            Width="22" 
            Height="120"
            BorderThickness="1,1,0,1" 
            BorderBrush="{ThemeResource AppBarBorderThemeBrush}" 
            Background="{ThemeResource SystemControlAcrylicElementBrush}" 
            Visibility="{x:Bind IsShow,Mode=OneWay}"           
            >
        <Thumb x:Name="Thumb" >
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Grid Background="Transparent">

                        <!--VisualState-->
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="BackgroundPointerOver" />
                                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="Background" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0" To="1" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="BackgroundPressed" />
                                        <DoubleAnimation Duration="0" To="0" Storyboard.TargetProperty="Opacity" Storyboard.TargetName="Background" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Border x:Name="Background"                              
                                IsHitTestVisible="False"                           
                                Width="6" 
                                Height="36" 
                                Margin="3" 
                                VerticalAlignment="Center" 
                                CornerRadius="{TemplateBinding CornerRadius}" 
                                BorderThickness="{TemplateBinding BorderThickness}"            
                                BorderBrush="{TemplateBinding BorderBrush}"            
                                Background="{TemplateBinding Background}"           
                                />
                        <Border x:Name="BackgroundPointerOver"                            
                                Opacity="0" 
                                IsHitTestVisible="False"
                                Width="6" 
                                Height="36" 
                                Margin="3" 
                                VerticalAlignment="Center" 
                                CornerRadius="{TemplateBinding CornerRadius}"          
                                BorderThickness="{TemplateBinding BorderThickness}"              
                                BorderBrush="{ThemeResource ThumbBorderBrushPointerOver}"             
                                Background="{ThemeResource ThumbBackgroundPointerOver}"              
                                />
                        <Border x:Name="BackgroundPressed"                              
                                Opacity="0" 
                                IsHitTestVisible="False"
                                Width="6" 
                                Height="36" 
                                Margin="3" 
                                VerticalAlignment="Center" 
                                CornerRadius="{TemplateBinding CornerRadius}"          
                                Background="{ThemeResource ThumbBackgroundPressed}"             
                                BorderBrush="{ThemeResource ThumbBorderBrushPressed}"              
                                BorderThickness="{TemplateBinding BorderThickness}"                
                                />
                    </Grid>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Border>

</Canvas>