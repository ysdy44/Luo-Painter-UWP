<elements:Expander
    x:Class="Luo_Painter.Menus.EditMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:elements="using:Luo_Painter.Elements"
    xmlns:local="using:Luo_Painter.Menus"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50"
    MinWidth="320" 
    BorderBrush="{ThemeResource AppStroke}" 
    Background="{ThemeResource SystemControlAcrylicElementBrush}" 
    >

    <elements:Expander.Title>
        Edit
    </elements:Expander.Title>

    <elements:Expander.TopAppBar>
        <ListBox SelectedIndex="{x:Bind FlipView.SelectedIndex, Mode=TwoWay}" >
            <ListBoxItem x:Name="EditItem">
                Edit
            </ListBoxItem>
            <ListBoxItem x:Name="SelectItem">
                Select
            </ListBoxItem>
            <ListBoxItem x:Name="CombineAndGroupItem">
                Combine &amp; Group
            </ListBoxItem>
            <ListBoxItem x:Name="SetupItem">
                Setup
            </ListBoxItem>
        </ListBox>
    </elements:Expander.TopAppBar>

    <UIElement.KeyboardAccelerators>
        <KeyboardAccelerator>
            <!--Without it, All controls will show ToolTip for KeyboardAccelerator-->
        </KeyboardAccelerator>
        <local:EditKeyboardAccelerator Modifiers="Control" Key="X" CommandParameter="Cut" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="J" CommandParameter="Duplicate" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="C" CommandParameter="Copy" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="V" CommandParameter="Paste" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Key="Back" CommandParameter="Clear" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="E" CommandParameter="Extract" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="M" CommandParameter="Merge" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="F" CommandParameter="Flatten" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="A" CommandParameter="All" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="D" CommandParameter="Deselect" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="I" CommandParameter="MarqueeInvert" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="P" CommandParameter="Pixel" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="R" CommandParameter="Group" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="U" CommandParameter="Ungroup" Command="{x:Bind Command}" />
        <local:EditKeyboardAccelerator Modifiers="Control" Key="R" CommandParameter="Release" Command="{x:Bind Command}" />
    </UIElement.KeyboardAccelerators>

    <FrameworkElement.Resources>
        <!--Command-->
        <local:OptionTypeCommand x:Name="Command"/>
    </FrameworkElement.Resources>

    <FlipView x:Name="FlipView" Height="328" Background="Transparent" >
        <FlipViewItem>
            <Grid ColumnSpacing="4" RowSpacing="4" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>

                <local:EditItem Type="Cut" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+X
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Duplicate" Grid.Column="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+J
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

                <local:EditItem Type="Copy" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+C
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Paste" Grid.Column="1" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+V
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

                <local:EditItem Type="Clear" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Back
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Extract" Grid.Column="1" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+E
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

                <local:EditItem Type="Merge" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+M
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Flatten" Grid.Column="1" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+F
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

            </Grid>
        </FlipViewItem>
        <FlipViewItem>
            <Grid ColumnSpacing="4" RowSpacing="4" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>

                <local:EditItem Type="All" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+A
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Deselect" Grid.Column="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+D
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

                <local:EditItem Type="MarqueeInvert" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+I
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Pixel" Grid.Column="1" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+P
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

                <local:EditItem Type="Feather" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="MarqueeTransform" Grid.Column="1" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

                <local:EditItem Type="Grow" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="Shrink" Grid.Column="1" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

            </Grid>
        </FlipViewItem>
        <FlipViewItem>
            <Grid ColumnSpacing="4" RowSpacing="4" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>

                <local:EditItem Type="Union" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="Exclude" Grid.Column="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

                <local:EditItem Type="Xor" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="Intersect" Grid.Column="1" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

                <local:EditItem Type="ExpandStroke" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="Release" Grid.Column="1" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+R
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

                <local:EditItem Type="Group" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+R
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>
                <local:EditItem Type="Ungroup" Grid.Column="1" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" >
                    <ToolTipService.ToolTip>
                        <ToolTip Style="{StaticResource AppToolTipStyle}">
                            Ctrl+U
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </local:EditItem>

            </Grid>
        </FlipViewItem>
        <FlipViewItem>
            <Grid ColumnSpacing="4" RowSpacing="4" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>

                <local:EditItem Type="CropCanvas" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="Stretch" Grid.Column="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

                <local:EditItem Type="FlipHorizontal" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="FlipVertical" Grid.Column="1" Grid.Row="1" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

                <local:EditItem Type="LeftTurn" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="RightTurn" Grid.Column="1" Grid.Row="2" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />
                <local:EditItem Type="OverTurn" Grid.Row="3" Height="Auto" VerticalAlignment="Stretch" Command="{x:Bind Command}" Style="{StaticResource AppBorderButtonStyle}" />

            </Grid>
        </FlipViewItem>
    </FlipView>
    
</elements:Expander>