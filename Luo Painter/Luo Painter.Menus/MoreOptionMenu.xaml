<elements:Expander
    x:Class="Luo_Painter.Menus.MoreOptionMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Luo_Painter.Menus"
    xmlns:elements="using:Luo_Painter.Elements"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50"
    MinHeight="329"
    MinWidth="320" 
    BorderBrush="{ThemeResource AppStroke}" 
    Background="{ThemeResource SystemControlAcrylicElementBrush}" 
    >

    <elements:Expander.Title>
        More Options
    </elements:Expander.Title>

    <FrameworkElement.Resources>
        <!--Command-->
        <local:OptionTypeCommand x:Name="OptionTypeCommand"/>
    </FrameworkElement.Resources>

    <StackPanel Spacing="2">
        <local:OptionItem Type="Transform" Command="{x:Bind OptionTypeCommand}" Style="{StaticResource AppBorderButtonStyle}" />
        <local:OptionItem Type="DisplacementLiquefaction" Command="{x:Bind OptionTypeCommand}" Style="{StaticResource AppBorderButtonStyle}" />
        <local:OptionItem Type="GradientMapping" Command="{x:Bind OptionTypeCommand}" Style="{StaticResource AppBorderButtonStyle}" />
        <local:OptionItem Type="RippleEffect" Command="{x:Bind OptionTypeCommand}" Style="{StaticResource AppBorderButtonStyle}" />
    </StackPanel>
</elements:Expander>