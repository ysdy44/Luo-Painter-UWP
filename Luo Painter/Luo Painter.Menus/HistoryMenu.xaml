<elements:Expander
    x:Class="Luo_Painter.Menus.HistoryMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:elements="using:Luo_Painter.Elements"
    xmlns:historys="using:Luo_Painter.Historys"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="400"
    MinWidth="320" 
    BorderBrush="{ThemeResource AppStroke}" 
    Background="{ThemeResource SystemControlAcrylicElementBrush}" 
    >

    <elements:Expander.Title>
        History
    </elements:Expander.Title>

    <ListView x:Name="ListView">

        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="historys:IHistory">
                <TextBlock>
                    <Run Text="{x:Bind Mode}"/>
                    <Run Text="{x:Bind Type}"/>
                </TextBlock>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        
    </ListView>

</elements:Expander>